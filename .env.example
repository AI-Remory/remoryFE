VITE_API_BASE_URL=/api/v1
REMORY_BACKEND_ORIGIN=http://141.164.48.128:8000

# VITE_API_BASE_URL is used by browser runtime code and must stay a same-origin
# relative path. Do not set it to the backend HTTP origin in Vercel or locally.
# Vercel Dashboard Environment Variables: VITE_API_BASE_URL=/api/v1
#
# REMORY_BACKEND_ORIGIN is for local Vite proxy/development configuration only.
# Browser code must not read or fetch this origin directly.
